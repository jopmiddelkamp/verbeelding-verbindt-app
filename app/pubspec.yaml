name: verbeelding_verbindt
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  verbeelding_verbindt_core:
    path: ../shared/core
  verbeelding_verbindt_data_firebase:
    path: ../shared/data_firebase
  verbeelding_verbindt_data_shared_preferences:
    path: ../shared/data_shared_preferences
  verbeelding_verbindt_ui:
    path: ui

  equatable: ^2.0.0
  firebase_core: ^1.0.0
  firebase_crashlytics: ^2.0.0
  get_it: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true