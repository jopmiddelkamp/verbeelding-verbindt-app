name: verbeelding_verbindt
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.14.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.0
  firebase_core: ^1.0.0
  firebase_crashlytics: ^2.0.0
  get_it: ^7.0.0
  verbeelding_verbindt_core:
    path: verbeelding_verbindt_core
  verbeelding_verbindt_data:
    path: verbeelding_verbindt_data
  verbeelding_verbindt_ui:
    path: verbeelding_verbindt_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.0.0
  build_runner: ^2.0.0
  flutter_lints: ^1.0.0
  json_serializable: ^6.0.0
  mocktail: ^0.2.0

flutter:
  uses-material-design: true