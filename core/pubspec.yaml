name: verbeelding_verbindt_core
description: A new Flutter package project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^7.0.0
  dartz: ^0.9.2
  equatable: ^2.0.0
  firebase_core: ^1.4.0
  firebase_crashlytics: ^2.1.1
  get_it: ^7.1.3
  rxdart: ^0.27.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^8.0.0
  build_runner: ^2.0.1
  flutter_lints: ^1.0.4
  json_serializable: ^4.0.3
  mocktail: ^0.1.0

flutter:
  uses-material-design: true
  generate: true