name: verbeelding_verbindt
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bot_toast: ^4.0.1
  device_info: ^2.0.0
  equatable: ^2.0.0
  get_it: ^7.1.3
  package_info: ^2.0.0
  verbeelding_verbindt_core:
    path: ../core
  verbeelding_verbindt_data:
    path: ../data
  verbeelding_verbindt_presentation:
    path: ../presentation

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^8.0.0
  build_runner: ^2.0.1
  effective_dart: ^1.3.1
  json_serializable: ^4.0.3
  mocktail: ^0.1.0

flutter:
  uses-material-design: true